import { redirect } from "next/navigation";

// This route is a template artifact — redirect to services index.
export default function PipelineCorrosionControlPage() {
  redirect("/craft-catalog");
}
